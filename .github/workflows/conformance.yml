name: conformance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  conformance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r harness/requirements.txt

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.21"

      - name: Build Go clean-room implementation
        run: |
          cd hacp-go
          go build -o hacp-go .

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build TypeScript clean-room implementation
        run: |
          cd hacp-ts
          npm install
          npm run build

      - name: Verify vector integrity (CI check)
        run: python tools/bake_vector.py --check

      - name: Run conformance (local mode)
        run: python harness/harness.py --mode local

      - name: Run conformance (Go clean-room, CLI)
        run: python harness/harness.py --mode cli --binary-path hacp-go/hacp-go

      - name: Run conformance (TypeScript clean-room, CLI)
        run: python harness/harness.py --mode cli --binary-path hacp-ts/dist/cli.js